package mbot_lcm_msgs;
// Motor duty cycle percent for the 
// 3 motors on the MBot Control Board
// The range is -1.0 to 1.0 where 
// the sign encodes motor direction
struct mbot_motor_pwm_t
{
    int64_t utime;
    float pwm[3]; // [-1.0..1.0]
}
